# Use an older Python version (deliberately vulnerable)
FROM python:3.7-slim  

WORKDIR /app

COPY app/ /app

# Install old vulnerable dependencies
RUN pip install flask==1.0.2 requests==2.19.1  

CMD ["python", "main.py"]
